services:
    dashboard:
        build:
            context: ../
            dockerfile: Dockerfile
        container_name: dashboard
        ports:
            - "3000:3000"
        environment:
            - MONGO_URI
            - INSTANCE_ID
